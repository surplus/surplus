name: "Test"
on: [push]

jobs:
  changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm i
      - run: npm run lint
      - run: npm run build
      - run: npm run test
      - run: npm run bench
